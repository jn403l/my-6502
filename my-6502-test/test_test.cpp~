#include <gtest/gtest.h>

namespace {
	int MyTest() { return 42; }
}

TEST(TestTopic, TrivialEquality) { EXPECT_EQ(MyTest(), 42); }

TEST(TestTopic, MoreEqualityTests) {
  ASSERT_EQ(MyTest(), 42) << "Oh no, a mistake!";
	EXPECT_FLOAT_EQ(23.24F, 23.24F);
}
