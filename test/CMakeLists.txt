if(BUILD_TESTING)
  add_executable(test_test test_test.cpp)
  target_link_libraries(test_test PRIVATE GTest::gtest_main)
  target_include_directories(test_test PRIVATE ../include)
  include(GoogleTest)
  gtest_discover_tests(test_test)
endif()
